<% content_for :title do %>
  Corsego | Editing <%= @course.title %>
<% end %>

<div class="card">
  <div class="card-header">
    <h3><i class="far fa-check-square"></i> Editing course</h3>
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
  <div class="card-footer">
    <%= link_to 'Show', @course %> |
    <%= link_to 'Back', courses_path %>
  </div>
</div>
